#include "main.h"

/**
 * _abs -> will compute the absolute value of a given integer
 * @n: the given  integer to be checked
 * Return: absolute value of the given checked integer
 */


int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}

	return (-n);
}
